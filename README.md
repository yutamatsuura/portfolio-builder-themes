# portfolio-builder themes (canonical)

@portfolio-builder エージェントが起動時に取得する「正典テーマ」の配布リポジトリ。
各 `themes/{id}.html` は完全自己完結の静的HTML（背景画像はbase64埋込）。
1行目付近に検証マーカー `<!-- pf-theme:{id} v1 -->` を持つ。

- light-aurora / aurora / atelier / light-sidebar
